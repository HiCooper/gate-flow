package com.gateflow.sdk.logger

/** Categories for log scoping. */
enum class LogScope {
    GateFlowCore,
    IdentityManager,
    ConfigManager,
    ExperimentManager,
    Network,
    Storage,
    Analytics,
    EventTracker,
    Delegate
}
